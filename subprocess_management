definition: The subprocess module in Python is used to run external programs and commands from within a Python script.


import subprocess

result= subprocess.run(["ls -l"], capture_output=True, text=True)
print(result.stdout)
